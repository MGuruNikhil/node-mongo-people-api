People Backend (Express + MongoDB)

Quick start

1. Install dependencies:

   cd backend
   npm install

2. Start MongoDB (local) and run the server:

   npm run dev

Defaults: connects to mongodb://127.0.0.1:27017/peopledb and listens on port 5000.
